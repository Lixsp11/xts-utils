[pytest]
testpaths = tests
pythonpath = .
addopts = -v -ra --tb=short
markers =
    crypto: password cryptography primitive tests
    rsakey: NSSSH private key parsing / conversion tests
    container: .xts container reader tests
    sshconfig: ssh-config rendering / export tests
    cli: command-line interface tests
